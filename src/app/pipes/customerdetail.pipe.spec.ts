import { CustomerdetailPipe } from './customerdetail.pipe';

describe('CustomerdetailPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomerdetailPipe();
    expect(pipe).toBeTruthy();
  });
});
