import { ProductdetailPipe } from './productdetail.pipe';

describe('ProductdetailPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductdetailPipe();
    expect(pipe).toBeTruthy();
  });
});
